<main>
	{% for post in collections.blog_posts %}
		{# Reminder - use article tag in the future here #}
		<h3>
			<a href="{{ post.url }}">{{ post.data.title }}</a>
			<small>{{ post.date.toUTCString().split(' 00')[0] }}</small> {# This is such a hack.... todo: fix #}
		</h3>
		<p>{{ post.data.description }}</p>
	{% endfor %}
</main>

<style type="text/css">
	main {
		flex: 1;
		padding: 1rem 0 1rem 0;
	}

	main h3 small {
		font-size: .75rem;
		font-weight: normal;
	}

	main p {
		margin-bottom: .75rem;
	}
</style>